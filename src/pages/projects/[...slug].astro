---
import { getCollection } from 'astro:content';
import Layout from '../../components/Layout.astro';
import TableOfContents from '../../components/TableOfContents.astro';
import { getUrl } from '../../utils/url';
import { formatDateMonthYear } from '../../utils/date';

export async function getStaticPaths() {
  const projectEntries = await getCollection('projects');
  return projectEntries.map(entry => ({
    params: { slug: entry.slug }, props: { entry },
  }));
}

const { entry } = Astro.props;
const { Content } = await entry.render();

// Function to try to import front picture
let frontPicture: string | null = null;
try {
  // Try to import front picture with common extensions
  const extensions = ['jpg', 'jpeg', 'png', 'webp', 'gif'];
  for (const ext of extensions) {
    try {
      const imported = await import(`../../content/projects/${entry.slug}/front-picture/front.${ext}`);
      if (imported && imported.default) {
        frontPicture = imported.default.src || imported.default;
        break;
      }
    } catch (e) {
      // Continue to next extension
    }
  }
} catch (e) {
  // Front picture not found
}
---

<Layout
  title={`${entry.data.title} - Projects`}
  description={entry.data.description}
  image={entry.data.image}
>
  <article class="project-detail">
    <header class="project-header">
      <div class="container">
        <h1>{entry.data.title}</h1>
        <p class="project-description">{entry.data.description}</p>
        
        <div class="project-meta">
          <div class="project-dates">
            <time datetime={entry.data.pubDate.toISOString()}>
              {formatDateMonthYear(entry.data.pubDate)}
            </time>
          </div>
          
          {entry.data.githubLink && (
            <div class="project-links">
              <a href={entry.data.githubLink} class="project-link github-link" target="_blank" rel="noopener noreferrer">
                <span>View on GitHub</span>
              </a>
            </div>
          )}
        </div>
        
        {entry.data.tags && entry.data.tags.length > 0 && (
          <div class="project-technologies">
            <h3>Technologies Used</h3>
            <div class="tech-tags">
              {entry.data.tags.map((tag: string) => (
                <span class="tech-tag">{tag}</span>
              ))}
            </div>
          </div>
        )}
      </div>
    </header>

    {entry.data.thumbnail && (
      <div class="project-thumbnail-section">
        <img src={entry.data.thumbnail} alt={`${entry.data.title} thumbnail`} class="project-thumbnail-image" />
      </div>
    )}
    
    <TableOfContents />
    
    <main class="project-content">
      <div class="container">
        <div class="content-with-sidebar">
          <div class="main-content prose-content">
            {entry.data.image && (
              <div class="project-image-section">
                <img 
                  src={entry.data.image} 
                  alt={`${entry.data.title} demonstration`}
                  class="project-demo-image"
                />
                <p class="image-caption">Project demonstration: {entry.data.title}</p>
              </div>
            )}
            <Content />
          </div>
          
          {frontPicture && (
            <div class="front-picture-sidebar">
              <h4>Project Preview</h4>
              <img 
                src={frontPicture} 
                alt={`${entry.data.title} preview`}
                class="front-picture-image"
              />
            </div>
          )}
        </div>
      </div>
    </main>
    
    <footer class="project-footer">
      <div class="container">
        <a href={getUrl("/projects/")} class="back-link">← Back to Projects</a>
      </div>
    </footer>
  </article>
</Layout>

<style>
  .project-detail {
    padding-top: 0;
  }
  
  .project-header {
    background: linear-gradient(135deg, var(--deep-navy), #1a3a68);
    color: var(--warm-neutral);
    padding: 6rem 0 5rem;
    position: relative;
    overflow: hidden;
  }
  
  .project-header h1 {
    font-size: 2.5rem;
    margin-bottom: 1.5rem;
    line-height: 1.1;
    text-shadow: 3px 3px 6px rgba(0, 0, 0, 0.3);
    font-weight: 800;
    color: var(--warm-neutral);
  }

  .project-thumbnail-section {
    text-align: center;
    margin-top: 3rem; /* Adjusted to push it down from the header */
    margin-bottom: 0;
  }

  .project-thumbnail-image {
    max-width: 70%; /* Reduced max-width for a slightly smaller image */
    height: auto;
    border-radius: 12px;
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.2);
    border: 2px solid var(--color-border);
  }

  
  .project-description {
    font-size: 1.4rem;
    margin-bottom: 3rem;
    opacity: 0.95;
    line-height: 1.6;
    color: var(--warm-neutral);
  }
  
  .project-meta {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 3rem;
    flex-wrap: wrap;
    gap: 2rem;
  }
  
  .project-dates {
    font-size: 1.2rem;
    opacity: 0.95;
    font-weight: 600;
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }
  
  .project-dates::before {
    content: '🚀';
    font-size: 1.3rem;
  }
  
  .project-links {
    display: flex;
    gap: 1.5rem;
    flex-wrap: wrap;
  }
  
  .project-link {
    background: var(--gold-accent);
    color: var(--deep-navy);
    padding: 1rem 2rem;
    border-radius: 15px;
    text-decoration: none;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    font-size: 0.9rem;
  }

  .project-technologies {
    border-top: 2px solid rgba(248, 247, 244, 0.3);
    padding-top: 3rem;
  }
  
  .project-technologies h3 {
    font-size: 1.4rem;
    margin-bottom: 1.5rem;
    opacity: 0.95;
    font-weight: 600;
    color: var(--warm-neutral);
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }
  
  .project-technologies h3::before {
    content: '🛠️';
    font-size: 1.2rem;
  }
  
  .tech-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
  }
  
  .tech-tag {
    background: var(--gold-accent);
    color: var(--deep-navy);
    padding: 0.7rem 1.3rem;
    border-radius: 25px;
    font-size: 0.9rem;
    font-weight: 700;
  }
  
  .project-content {
    padding: 2rem 0;
    max-width: 900px;
    margin: 0 auto;
  }
  
  .project-content .container {
    padding: 2.5rem;
    border-radius: 16px;
    margin: 2rem 0;
    line-height: 1.8;
    color: var(--deep-navy);
    font-family: var(--font-accent);
    font-size: 1.1rem;
  }

  /* Prose styles handled by shared .prose-content class in content.css */
  
  .project-image-section {
    margin: 3rem 0 4rem 0;
    text-align: center;
  }
  
  .project-demo-image {
    width: 100%;
    max-width: 800px;
    height: auto;
    border-radius: 16px;
    box-shadow: 0 12px 32px rgba(0, 0, 0, 0.15);
  }
  
  .image-caption {
    margin-top: 1.5rem;
    color: var(--color-text-light);
    font-family: var(--font-accent);
    font-size: 1.1rem;
    font-style: italic;
    font-weight: 500;
  }
  
  .project-footer {
    background: var(--deep-navy);
    padding: 3rem 0;
    border-top: 3px solid var(--gold-accent);
  }
  
  .back-link {
    color: var(--warm-neutral);
    text-decoration: none;
    font-weight: 600;
    font-size: 1.1rem;
  }

  @media (max-width: 1200px) {
    .project-header .container,
    .project-content,
    .project-footer .container {
      margin-left: auto;
      margin-right: auto;
      max-width: 900px;
      padding-left: 2rem;
      padding-right: 2rem;
    }
  }
  
  @media (max-width: 768px) {
    .project-header h1 {
      font-size: 2rem;
    }
    
    .project-description {
      font-size: 1.1rem;
    }
    
    .project-meta {
      flex-direction: column;
      align-items: flex-start;
    }
    
    .project-links {
      width: 100%;
      justify-content: center;
    }
    
    .project-header .container,
    .project-content,
    .project-footer .container {
      padding-left: 1rem;
      padding-right: 1rem;
    }

    .content-with-sidebar {
      gap: 1.5rem;
    }

    .front-picture-sidebar {
      padding: 1.5rem;
      max-width: none;
    }
  }
</style>


 